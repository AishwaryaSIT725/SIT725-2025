// Placeholder model file for future MongoDB integration with Mongoose.
// Example schema (commented out) for reference:
/*
const mongoose = require('mongoose');
const bookingSchema = new mongoose.Schema({
  studentId: String,
  tutorId: String,
  subject: String,
  date: Date,
  status: { type: String, default: 'pending' }
});
module.exports = mongoose.model('Booking', bookingSchema);
*/
